# Buutti Fullstack Exercise

## Description

A simple app that has a form to insert books into a list. Frontend was done with React and backend uses Node and MongoDB.

## Running the app

Navigate to the server folder and run `npm start`
